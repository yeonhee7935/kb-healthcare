# true | false — controls whether the MSW mock worker starts on app boot
VITE_USE_MOCK=true

# Base URL prefixed to every API call. Leave empty for same-origin (dev + mock).
# e.g. https://api.kb-healthcare.example
VITE_API_BASE_URL=
