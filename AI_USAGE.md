# AI 활용 기록

## 사용 여부

- 사용함
- 도구 및 모델: Claude Code (Sonnet 5)
- 활용 방식: 요구사항 해석과 기술 선택은 직접 판단하고, 스펙 분석·원인 진단·대안 비교처럼 시간이 걸리는 작업에 Claude Code를 활용해 근거를 빠르게 확보한 뒤 최종 결정했습니다.

## 활용한 작업

| 작업              | 진행 방식                     | 결과                              |
| :---------------- | :---------------------------- | :-------------------------------- |
| 요구사항/API 파악 | 문서 전달 후 범위 정리 요청   | 이후 논의 기준으로 사용           |
| 기술 스택 선정    | 영역별 후보/트레이드오프 요청 | react-router 등 조합 확정         |
| 스캐폴딩·구현     | 확정 스택으로 구현 요청       | 자동 검사 + 브라우저 확인 후 반영 |
| UI QA             | 스크린샷 공유, 원인 진단 요청 | 검토 후 수정 방식 결정            |

## 주요 판단

### 1. 인증 토큰 관리

**채택한 제안**

- 제안: accessToken은 브라우저 메모리에만 두고, refreshToken은 JS로 읽을 수 없는 httpOnly 쿠키에 저장
- 채택 이유: 스펙의 `bearerAuth`(Authorization 헤더로 전달)와 `refreshTokenCookie`(쿠키로 전달) 구분과 맞아떨어지고, accessToken을 localStorage 등에 저장할 때 생기는 XSS 토큰 탈취 위험을 줄일 수 있음

### 2. 스펙에 없는 기능 처리 (할 일 추가·완료 토글·로그아웃)

**채택한 제안**

- 제안: 할 일 추가·완료 토글·로그아웃 3개는 `openapi.yaml`에 없는 endpoint라 MSW mock에만 추가해 구현하고, README에 이 사실을 명시
- 채택 이유: 세 기능 모두 할 일 관리 앱에서 빠지면 안 되는 기능으로 판단함 — 로그아웃은 비로그인 상태로 전환해 인증 가드가 의도대로 동작하는지 확인하는 데 필요하고, 완료 토글은 "할 일/한 일"을 구분하는 할 일 목록의 핵심 동작이며, 할 일 추가는 목록을 채우는 가장 기본적인 기능임. 스펙에 없다는 이유로 빼기보다, 할 일 관리라는 프로젝트 목적에 맞게 요구사항을 보완하는 쪽으로 판단함

**거절하거나 다시 요청한 제안**

- 제안: 세 기능을 구현하지 않고 보류하거나, `openapi.yaml` 스펙 파일 자체에 endpoint를 추가
- 문제점: 보류하면 할 일 추가/완료 처리/로그아웃을 화면에서 확인할 수 없고, 스펙 파일을 임의로 고치면 "주어진 API 계약을 따른다"는 전제가 깨짐
- 최종 결정: mock 서버에만 endpoint를 추가하는 절충안 채택

### 3. 인증 가드 리다이렉트 방식

**채택한 제안**

- 제안: 비로그인 상태로 보호된 라우트(`/task` 등)에 접근했을 때, 로그인 페이지로 즉시 이동시키는 대신 같은 화면 안에서 "로그인이 필요합니다" 안내와 로그인 버튼을 바로 보여줌
- 채택 이유: 기존 방식(`<Navigate>`로 로그인 페이지로 이동)은 페이지가 이동했다가 로그인 페이지가 새로 그려지는 과정에서 화면이 한 번 깜빡였는데, 페이지 이동 자체를 없애면 이 문제가 사라짐. 이 가드(`RequireAuth`)는 할 일 목록·상세 등 보호된 모든 라우트에 공통으로 걸려 있어 한 곳만 고치면 전체에 적용됨. 또한 다른 화면으로 이동시켰다가 로그인 페이지를 보여주는 것보다, 접근하려던 화면 그 자리에서 왜 접근할 수 없고 어떻게 하면 되는지를 바로 보여주는 편이 사용자 입장에서 더 자연스럽다고 판단함

**거절하거나 다시 요청한 제안**

- 제안: 화면은 그대로 이동시키되, "로그인이 필요합니다"라는 토스트/스낵바 알림만 추가로 띄우는 방식
- 문제점: 페이지 이동으로 인한 깜빡임 자체는 사라지지 않고, 알림은 놓치기 쉬운 부가 정보일 뿐 원인 해결책이 아님
- 최종 결정: 인라인 안내로 통일 (한 곳 수정으로 보호된 모든 라우트에 적용되고, 화면 이동이 없어 깜빡임도 없음)

## 검증

- 실행한 자동 검사: typecheck, lint, vitest, build
- 사용자 시나리오: "로그인 후 원래 경로 복귀" 코드 위치 확인 후 적용, `/task/7` 접속 → 로그인 → 원래 경로 복귀 및 히스토리 왕복 없음 확인, 새로고침 후 세션 유지 확인, 로그아웃 후 재로그인 안 됨 확인
- 명세와 대조한 내용: `openapi.yaml`의 `bearerAuth`/`refreshTokenCookie` 스킴과 토큰 저장 방식 일치 여부 확인

## 알려진 한계 / 남은 이슈

- 할 일 추가(`POST /api/task`), 완료 토글(`PATCH /api/task/:id`), 로그아웃(`POST /api/sign-out`)은 `openapi.yaml`에 없는 endpoint로, MSW mock에만 구현됨
- mock 데이터는 메모리에만 저장되어, 새로고침하면 시드 데이터로 초기화됨 — 추가·삭제·완료 토글한 내용은 새로고침 전까지만 유지됨
